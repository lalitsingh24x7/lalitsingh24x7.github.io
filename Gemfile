source "https://rubygems.org"

# Use github-pages gem to match the exact versions GitHub Pages uses
gem "github-pages", group: :jekyll_plugins

group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-seo-tag"
end
